A basic example to generate butane snippets for common operating system configuration for then using them as ignition configuration. 

## Usage

To run this example you need to execute:

```bash
terraform init
terraform plan
terraform apply
```
