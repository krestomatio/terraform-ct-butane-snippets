A basic example to generate butane snippets to configure certbot for then using it as ignition configuration.
## Usage

To run this example you need to execute:

```bash
terraform init
terraform plan
terraform apply
```
